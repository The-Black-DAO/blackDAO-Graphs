{
  "name": "blkd-subgraph",
  "version": "0.1.0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy-hosted": "graph deploy --product hosted-service wasif28/Black-Dao"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.27.0",
    "@graphprotocol/graph-ts": "^0.25.0"
  }
}
